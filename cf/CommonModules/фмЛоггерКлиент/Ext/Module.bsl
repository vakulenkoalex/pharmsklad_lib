
#Область СлужебныеПроцедурыИФункции

#Если Не ВебКлиент Тогда

// Нужно использовать фмЛоггерКлиентСервер.СоздатьЛоггер.
//
Функция СоздатьЛоггер(Знач ИерархияСобытия) Экспорт
	
	Контейнер = ПолучитьФорму("Обработка.фмЛоггер.Форма.ЗаписьНаКлиенте");
	Контейнер.ИерархияСобытия = ИерархияСобытия;
	Контейнер.ИмяКлиента = ИмяКомпьютера();
	
	Возврат Контейнер;
	
КонецФункции

#КонецЕсли

#КонецОбласти
